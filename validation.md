# Validação do Projeto Ulisses

| Área | Evidência | Resultado |
|---|---|---|
| Titularidade de rotas | Testes de integração organizacional | Atribuição por e-mail e escopo de liderança preservados. |
| Super Pipe | `server/salesPipeline.test.ts` | Estágios comerciais, conversões e prioridade de oportunidades quentes validados. |
| Regressão | `pnpm check && pnpm test` | **20 arquivos de teste e 45 testes aprovados**. |

| Painel de gestão | Captura desktop após o checkpoint integrado | Cards de TPV, novos clientes, agentes em meta e filtros de equipe renderizados sem sobreposição. |

| Área | Evidência | Resultado |
|---|---|---|
| Histórico de carteiras | `server/organization.integration.test.ts` | Seis competências de Base e Funil do Pipe são retidas e selecionadas corretamente, com isolamento por rota formal. |
| Titularidade formal | `server/organization.integration.test.ts` | Perfil com rota sem atribuição não obtém leitura ou importação; vínculo por e-mail preserva a rota para o agente. |
| Super Pipe | Captura desktop `?view=super-pipe` e teste de escopo | Navegação de liderança, filtros, estágios, conversões e estado vazio renderizados com dados restritos ao Funil do Pipe autorizado. |
| Carteiras | Captura desktop `?view=carteiras` | Seletor de competência, importação hierárquica e titularidade de rota renderizados sem sobreposição. |
| Ranking e Tio Patinhas | Captura desktop `?view=ranking` | Tabela anônima inclui as faixas 15–30k e 30–50k, com estado vazio claro quando não existem cards do mês. |
| Visão distrital | Captura desktop do painel e teste organizacional | Seção separada lista somente lideranças do distrito; lideranças externas são excluídas por teste. |
| Responsividade móvel | Capturas móveis de Super Pipe, Carteiras, Ranking e painel distrital | Navegação inferior, filtros, cards, tabelas com rolagem horizontal e estados vazios permanecem legíveis em 375 px. |
| Cavalo de Tróia | `server/prospection.integration.test.ts` | Dossiês são privados, leads de carteira exigem rota formalmente atribuída e a consulta pública de CNPJ é testada com fonte rastreável. |
| Cavalo de Tróia — UI | Capturas desktop e móvel `?view=prospeccao` | O formulário, avisos de privacidade, prioridades de carteira e estados vazios estão legíveis e não sugerem contato automático. |
| Remetente de recuperação | Consulta Resend, DNS público e entrega autorizada em 27/08/2026 | DKIM, SPF e MX propagaram; `novaodisseia.com` ficou **verified** no Resend. A mensagem de validação foi aceita pelo provedor e recebida em `fiorati@novaodisseia.com`, conforme confirmação do usuário. |
| Entrega corporativa Stone | Envio autorizado e confirmação do usuário em 27/08/2026 | A mensagem de validação enviada para `gabriel.fmarcantonio@stone.com.br` foi aceita pelo provedor e confirmada como recebida. O remetente verificado está apto ao fluxo de recuperação. |
| Recuperação de senha — fluxo real | Solicitação autorizada e confirmação do usuário em 27/08/2026 | `auth.requestPasswordReset` retornou sucesso e o código temporário de seis dígitos foi confirmado como recebido na caixa Stone autorizada. A senha não foi alterada durante a validação. |
| Carteiras históricas — estado vazio | Captura autenticada desktop em 27/08/2026 | O seletor de competência, o aviso de titularidade formal e o estado vazio por competência ficaram visíveis e legíveis na área Carteiras. |
| Ritual PSV | Capturas autenticadas desktop e móvel em 27/08/2026 | A cadência de 20–30 oportunidades, os campos de resultado/plano do dia, o roteiro semanal e a preparação de leads do funil ficaram legíveis e responsivos. |
| Suíte consolidada | `pnpm check && pnpm test` em 27/08/2026 | Checagem de tipos aprovada; 28 arquivos de teste e 66 testes aprovados. Um teste externo de e-mail permaneceu opt-in e foi ignorado, pois a entrega real foi validada separadamente com confirmação do usuário. |
| Campanhas de engajamento | Teste de integração e capturas desktop/móvel em 27/08/2026 | A criação é limitada à liderança, o escopo é forçado ao polo/distrito autorizado, a leitura distrital foi validada e agentes comuns foram bloqueados. A projeção usa somente agentes elegíveis e metas declaradas; resultados permanecem preenchimento manual. |
| Identidade Jornada do Herói - Stone | Checagem de tipos, teste de ambiente e captura de acesso em 27/08/2026 | Título do navegador, pacote, lockup de autenticação e rótulos de navegação foram atualizados; o domínio compartilhado foi preservado. |
| Endurecimento de acesso | `server/security.integration.test.ts` em 27/08/2026 | Agentes não conseguem se promover a liderança, cadastros públicos nascem como agentes, liderança não altera o próprio escopo e uma rota não pode ser atribuída a perfil já vinculado a outro polo/distrito. |
| Auditoria operacional | Migração `0015_plain_dorian_gray.sql` aplicada e teste de integração | Criação/edição de campanhas, atribuição de rota e importação de carteiras registram somente ator, tipo, alvo, escopo e horário; senhas, planilhas, clientes e texto de campanha não são copiados. |
| Superfície HTTP | `server/_core/requestSecurity.test.ts` e validação por HTTP local | Mutações de origem externa retornaram **403**; a mesma origem alcançou a validação normal da API. Cabeçalhos anti-clickjacking, anti-MIME sniffing, política de permissões e anti-cache de API foram observados na prévia. |
| Build de produção | `pnpm build` em 27/08/2026 | Build concluído sem source maps publicados. |
| Suíte final | `pnpm check && pnpm test` em 27/08/2026 | **32 arquivos aprovados, 77 testes aprovados e 1 teste externo de e-mail opt-in ignorado**; build de produção concluído. |
| SPARTACUS — seleção de competências | Capturas autenticadas em desktop e celular em 27/08/2026 | A aba está disponível para Distrital, Dono de Polo e Agente. Os 20 cartões de hard skills e os 20 cartões de soft skills permanecem legíveis; cada grupo indica a seleção obrigatória de três competências. |
| SPARTACUS — PDI e privacidade | `shared/spartacus.test.ts` e `server/spartacus.integration.test.ts` em 27/08/2026 | O catálogo contém 20 hard skills e 20 soft skills, o gerador exige três diferentes em cada grupo, produz quatro semanas, áudio local e prompt com limites de privacidade. O PDI é gravado e listado exclusivamente para o próprio usuário; o papel é derivado do perfil no servidor. |
| SPARTACUS — suíte final | `pnpm check && pnpm test && pnpm build` em 27/08/2026 | **33 arquivos aprovados, 79 testes aprovados e 1 teste externo opt-in ignorado**. Build de produção concluído sem source maps. |
| Nova Odisseia — prévia visual | Capturas autenticadas desktop e celular em 27/08/2026 | O card de propósito, os oito pilares de desenvolvimento e o painel administrativo global foram renderizados; o lockup herdado foi encaminhado para substituição pelo novo emblema na validação final. |
| Nova Odisseia — lockup entregue | Reinício da prévia e leitura de `http://localhost:3000/src/pages/Home.tsx` em 27/08/2026 | O código-fonte entregue pelo servidor contém exclusivamente os dois lockups `NOVA ODISSEIA`; o emblema herdado foi substituído por asset sem texto da nova identidade. |
| Administração Nova Odisseia | `server/admin.integration.test.ts` em 27/08/2026 | A conta administrativa autorizada é provisionada por configuração protegida, recebe papel global `admin` e permanece exceção permitida ao cadastro fora do domínio Stone. Senhas não foram gravadas em código ou em logs. |
| Gestão administrativa | `server/admin.integration.test.ts` e `server/security.integration.test.ts` | Usuários comuns são bloqueados na alteração de hierarquia e metas; a conta administrativa pode ajustar perfil, polo, distrito, rota e metas-base. Alterações são registradas pela trilha de auditoria. |
| Identidade Nova Odisseia | `server/appIdentity.test.ts`, capturas responsivas e build de produção | O título configurado é retornado pelo contrato público, metadados e tela de entrada foram atualizados e o card de propósito foi renderizado em desktop e celular. |
| Validação final Nova Odisseia | `pnpm check && pnpm test && pnpm build` em 27/08/2026 | **33 arquivos aprovados, 80 testes aprovados e 1 teste externo opt-in ignorado**. Build de produção concluído sem source maps. |
| Nova Odisseia — correção de wordmark | Revisão visual móvel e ajuste de `platform.css` em 27/08/2026 | Uma camada visual herdada ainda sobrepunha o wordmark anterior. A sobreposição foi atualizada para `NOVA` e `ODISSEIA: FIORATI`, incluindo o cabeçalho móvel, e foi confirmada na prévia responsiva. |
| Progresso e notificações de PDI | `server/spartacus.integration.test.ts` em 27/08/2026 | Um PDI novo cria seis trilhas privadas iniciadas em 0% e uma notificação individual. O avanço é registrado em etapas de 25%, validado contra o PDI do próprio usuário; terceiros não acessam nem alteram progresso ou avisos. |
| Exportação privada | `server/printDocument.test.ts` e `pnpm build` em 27/08/2026 | PDI, Ritual PSV e RMR abrem documento A4 local para “Salvar como PDF”; o HTML escapa conteúdo informado e não busca nem inclui dados de outros usuários. |
| Interface responsiva | Capturas desktop do painel e móvel de SPARTACUS em 27/08/2026 | O painel mantém a área de acompanhamento para agentes e a caixa de notificações; a seleção SPARTACUS ficou legível em 375 px. |
| Suíte final de progresso | `pnpm check && pnpm test && pnpm build` em 27/08/2026 | **34 arquivos aprovados, 81 testes aprovados e 1 teste externo opt-in ignorado**. Build de produção concluído. |
| Mascote Fiodisseu | Revisão visual desktop e celular em 27/08/2026 | O mascote foi gerado como PNG com alpha transparente, sem cenário de fundo, e aplicado ao ícone do shell. O cabeçalho móvel foi ajustado para separar o mascote do wordmark Nova Odisseia. |
| Aparição decorativa | Revisão de `platform.css` e prévia responsiva | Fiodisseu surge de maneira temporizada em cantos da interface, com `pointer-events: none` para não cobrir controles e tratamento para redução de movimento do sistema operacional. |
| Validação final Fiodisseu | `pnpm check && pnpm test && pnpm build` em 27/08/2026 | **34 arquivos aprovados, 81 testes aprovados e 1 teste externo opt-in ignorado**. Build de produção concluído. |
| Lista Inteligente — revisão responsiva | Capturas do painel autenticado em desktop e celular em 27/08/2026 | O shell, os controles administrativos e a navegação permanecem legíveis em 1280 px e 375 px após a inclusão da nova área de Carteiras. A importação por link está condicionada a rota formalmente atribuída e não foi executada com dados reais durante a validação. |
| Lista Inteligente — origem e normalização | `server/stoneLeadList.test.ts` e `server/portfolioFileParsing.test.ts` em 27/08/2026 | A importação aceita exclusivamente HTTPS em domínio `stone.com.br` ou subdomínio, sem porta alternativa ou redirecionamento. CSV, XLS e XLSX são normalizados para cliente, telefone, cidade, etapa, valor, última interação e próxima ação. |
| Lista Inteligente — escopo e deduplicação | `server/stoneLeadList.integration.test.ts` em 27/08/2026 | Uma exportação simulada, sem dados reais, foi importada somente para rota formalmente atribuída; duplicatas por documento, telefone ou cliente/cidade foram removidas. Outro agente não recebeu os registros. A URL completa não é persistida. |
| Lista Inteligente — suíte final | `pnpm check && pnpm test && pnpm build` em 27/08/2026 | **36 arquivos aprovados, 85 testes aprovados e 1 teste externo opt-in ignorado**. Build de produção concluído; os avisos de tamanho de bundle e URLs de ativos estáticos não impedem a publicação. |
| ÍTAKA — base autorizada | Auditoria de `banco_mensagens_itaka.xlsx` em 27/08/2026 | As abas **Banco de Mensagens** e **Instruções de Uso** foram encontradas. A base tem 365 chaves válidas de 1 a 365, os cinco campos esperados, nenhuma lacuna e nenhuma frase motivacional duplicada. O dia 29/02 repete deliberadamente a chave 365. |
| ÍTAKA — conteúdo e referência | Revisão do arquivo fornecido e contrato `shared/itakaDailyMessage.ts` | A interface usa somente a frase, a paráfrase fornecida, o tema e a referência por **Canto**; não consulta nem reproduz traduções publicadas da Odisseia. |
| ÍTAKA — execução privada | Verificação de execução em 27/08/2026 | O servidor carregou a base pelo armazenamento configurado e retornou somente a mensagem do dia, com data `27/08/2026`, referência `Canto II` e tema `coragem`. A consulta exige sessão autenticada. |
| ÍTAKA — interface | Capturas desktop e móvel do shell autenticado em 27/08/2026 | O shell permaneceu legível em 1280 px e 375 px. A sessão de validação era de liderança, portanto não se alterou perfil nem dado real para forçar a visão de agente; o card é coberto por tipagem, teste determinístico e verificação de execução. |
| ÍTAKA — suíte final | `pnpm check && pnpm test && pnpm build` em 27/08/2026 | **37 arquivos aprovados, 87 testes aprovados e 1 teste externo opt-in ignorado**. Build de produção concluído; os avisos de tamanho de bundle e ativos estáticos não impedem a publicação. |
| Período — comparativo semestral | `server/semesterComparison.test.ts` e `server/monthlyJourney.integration.test.ts` em 27/08/2026 | A seleção retorna exatamente seis meses por semestre. O histórico vem apenas do usuário autenticado, preserva meses sem registros como vazios e prioriza o Card Final quando ele consolida o TPV da competência. |
| Período — gráfico de barras | Revisão de `QuickPeriodPanel.tsx` e checagem de tipos | O topo da aba mostra barras de TPV realizado e meta de TPV, seletor de semestre e estado vazio explícito. Um resumo textual oculto torna os seis valores disponíveis a leitores de tela. A sessão disponível era de liderança; nenhum perfil foi alterado para forçar a tela de agente. |
| Período — suíte final | `pnpm check && pnpm test && pnpm build` em 27/08/2026 | **38 arquivos aprovados, 89 testes aprovados e 1 teste externo opt-in ignorado**. Build de produção concluído; avisos de tamanho de bundle e ativos estáticos não impedem a publicação. |
| Remuneração M0–M2 — fórmula | `server/migrationRemuneration.test.ts` em 27/08/2026 | O cenário informado separa R$ 1.505,00 de base de novos M0→M1 e R$ 475,00 de migração tardia M−2. Com 1,4x, resulta em R$ 2.772,00. O TPV tardio é exibido e não alimenta o TPV relevante da meta. |
| Remuneração Hunter — card | `server/migrationRemuneration.test.ts` em 27/08/2026 | Para TPV de R$ 1.130.735,06 e alíquota de 0,10%, o resultado de R$ 1.438,29 é reproduzido pelo atingimento bruto de 127,2% do KPI, não pelo degrau de 1,4x aplicado à venda nova. A base aceita 0,08%, 0,09% e 0,10% editáveis. |
| Remuneração — memória e responsividade | Checagem de tipos e capturas do shell em 27/08/2026 | A calculadora agora registra M0, M1, percentual de migração, origem M−2 e memória por componente. O shell permaneceu legível em 1280 px e 375 px; a sessão disponível era de Distrital e não foi alterada para forçar dados de agente. |
| Remuneração — suíte final | `pnpm check && pnpm test && pnpm build` em 27/08/2026 | **39 arquivos aprovados, 91 testes aprovados e 1 teste externo opt-in ignorado**. Build de produção concluído; os avisos de tamanho de bundle e ativos estáticos não impedem a publicação. |
| ÍTAKA — correção de contraste | Revisão de `ItakaDailyWelcome.tsx` e `platform.css` em 27/08/2026 | A passagem agora usa foreground explícito `#f4fbea` sobre `#073725`; rótulo e citação usam `#d9f98a`, e o tema mantém fundo claro com texto escuro. Isso evita a herança de verdes de baixa legibilidade observada no card. |
| ÍTAKA — validação de contraste | `pnpm check`, `server/itakaDailyMessage.test.ts` e `pnpm build` em 27/08/2026 | Tipagem aprovada, 2 testes da mensagem diária aprovados e build de produção concluído. A sessão de prévia disponível segue sendo de liderança; não houve alteração de perfil ou de dados privados para forçar a visão de agente. |
| Lista Inteligente — aba direta | `server/listIntelligentNavigation.test.ts`, `server/stoneLeadList.test.ts` e `server/stoneLeadList.integration.test.ts` em 27/08/2026 | A rota dedicada é reconhecida exclusivamente pela navegação. A tela reutiliza a mesma mutação autorizada, com validação HTTPS Stone, ausência de redirecionamentos, deduplicação e restrição por rota. |
| Lista Inteligente — interface | Capturas autenticadas em 1280 px e 375 px em 27/08/2026 | A nova aba fica visível e abre diretamente a área de importação. Para uma conta sem rota formal, o aviso de titularidade é mostrado e o campo de link permanece protegido; não foram inseridos dados de carteira durante a validação. |
| Domínio personalizado | DNS, HTTPS e HTML público de `www.novaodisseia.com` em 28/08/2026 | O subdomínio resolve para `cname.manus.space`, responde em HTTPS com cabeçalhos de segurança e entrega o título **Nova Odisseia: Fiorati**. O domínio compartilhado `calc-rv-6wafhwha.manus.space` permanece ativo como contingência. |
| Validação consolidada | `pnpm check && pnpm test && pnpm build` em 28/08/2026 | **40 arquivos aprovados, 92 testes aprovados e 1 teste externo opt-in ignorado**. O build de produção foi concluído sem arquivos source map publicados. |
| Lista Inteligente — host Google oficial | `server/stoneLeadList.test.ts` em 28/08/2026 | O allowlist aceita apenas HTTPS em host efêmero com prefixo hexadecimal e sufixo `-apidata.googleusercontent.com`, bucket `cartao-super-zap-1`, caminho `tam-leads-report`, nome `.html`, assinatura `jk` e `isca=1`. Bucket, extensão, assinatura ou parâmetros diferentes são bloqueados. |
| Lista Inteligente — parser HTML | `server/portfolioFileParsing.test.ts` em 28/08/2026 | Uma tabela HTML estática é convertida nos mesmos campos de cliente, telefone, cidade, etapa, TPV, última interação e próxima ação. Scripts, estilos e templates são removidos sem execução; HTML sem tabela reconhecível é rejeitado. |
| Lista Inteligente — segurança e rota | `server/stoneLeadList.integration.test.ts` em 28/08/2026 | O download simulado usa `redirect: manual`, deduplica a carteira, restringe a leitura à rota formal, grava somente `apidata.googleusercontent.com` como origem e comprova que a assinatura temporária não aparece nos metadados. Nenhuma chamada foi feita ao link real. |
| Lista Inteligente HTML — suíte final | `pnpm check && pnpm test && pnpm build` em 28/08/2026 | **40 arquivos aprovados, 94 testes aprovados e 1 teste externo opt-in ignorado**. Build concluído sem source maps; apenas avisos já conhecidos de bundle e ativos de runtime. |
