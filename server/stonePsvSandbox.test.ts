import {describe,expect,it} from 'vitest';import {SEED,filterLeads,focusFlag,parseCsv,previewCsv} from '../client/stone-psv/model';
describe('Stone PSV sandbox',()=>{it('filters stage, route and agent without changing source',()=>{expect(filterLeads(SEED,'Credenciado','Rota Leste','Consultor B').map(x=>x.id)).toEqual(['EX-04']);expect(SEED).toHaveLength(5)});it('keeps an absent TPV distinct from zero',()=>{expect(focusFlag(SEED[4])).toBe('TPV pendente');expect(focusFlag({...SEED[4],tpv:0})).toBe('Fora do foco 100k-300k')});it('parses quoted commas and flags missing TPV',()=>{const r=previewCsv('id,cliente,rota,agente,etapa,tpv,acao,data\n1,"Empresa, Exemplo",Norte,A,Mapeado,,Visita,2026-09-28');expect(r.errors).toEqual([]);expect(r.warnings[0]).toMatch(/TPV vazio/);expect(r.sample[0].cliente).toBe('Empresa, Exemplo')});it('blocks duplicate IDs and missing columns',()=>{let p=previewCsv('id,cliente\n1,A\n1,B');expect(p.errors.join(' ')).toMatch(/ID repetido/);expect(p.errors.join(' ')).toMatch(/tpv/)});it('rejects malformed CSV',()=>{expect(()=>parseCsv('id,cliente\n1,"Unclosed')).toThrow(/Aspas/)})});
